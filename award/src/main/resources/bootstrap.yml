server:
  port: ${PORT:8082}

spring:
  application:
    name: award

---
spring:
    profiles: cloud
eureka:
  instance:
    hostname: ${vcap.application.uris[0]}
    nonSecurePort: 80c
  client:
      serviceUrl:
        defaultZone: http://service-registry.195.13.10.14.xip.io/eureka